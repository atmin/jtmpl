/*

*/

    store = new Store();

    function getsetFactory(obj, caller) {
      var proto = {
        watchers: {}
      };

      return function(prop, val) {
        // Getter?
        if (val === undefined) {

        }
        // Setter
        else {

        }
      };
    }